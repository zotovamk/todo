import { RpcActionDirective } from './rpc-action.directive';

describe('RpcActionDirective', () => {
  it('should create an instance', () => {
    const directive = new RpcActionDirective();
    expect(directive).toBeTruthy();
  });
});
