import { WithoutComplitedArrDirective } from './without-complited-arr.directive';

describe('WithoutComplitedArrDirective', () => {
  it('should create an instance', () => {
    const directive = new WithoutComplitedArrDirective();
    expect(directive).toBeTruthy();
  });
});
